# Docker Compose Environment Configuration
# Used by docker-compose.yml

# Environment
ENVIRONMENT=docker
DEBUG=false

# JWT Configuration
JWT_SECRET=docker-jwt-super-secret-key-production-ready-2025
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# User Management Service (Docker Network)
USER_SERVICE_URL=http://am-user-management:8000
USER_SERVICE_TIMEOUT=30

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Auth Tokens Service
VERSION=1.0.0

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Docker Network Configuration
COMPOSE_PROJECT_NAME=am-services
NETWORK_NAME=am-network